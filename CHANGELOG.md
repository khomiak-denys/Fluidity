## Unreleased

- fix(ui): restore drink emoji icons (use unicode escapes for encoding-safety)
- refactor(screens): use relative import for time_format.dart

- fix(widgets): use unicode escape for water droplet fallback in `WaterIntakeCard`
- fix(screens): use unicode escape for water droplet fallback in `WaterEntryDetailScreen`
- docs(utils): add import-style comment to `time_format.dart`
- docs(changelog): record additional small fixes
